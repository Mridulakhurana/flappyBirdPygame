#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */     
    int n, i, arr[];
    scanf("%d", &n);
    if(n%2==0)
    {
        for(i=1; i<=n; i++)
        {
            arr[i]= i*11
            print("%d", arr[i])
        }    
    }
    
    
    
    
    return 0;
}
